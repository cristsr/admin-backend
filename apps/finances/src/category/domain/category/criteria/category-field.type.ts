/**
 * Every category attribute a criteria may name. `isSystem` stays internal — it
 * is a seed detail, not a facet to browse by.
 */
export type CategoryField = 'id' | 'name' | 'icon' | 'color' | 'isSystem' | 'createdAt';
