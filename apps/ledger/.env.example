ENV=local
PORT=3100
DB_TYPE=postgres
DB_URI=postgres://postgres:admin@localhost:5433/ledger
DB_SSL=false
DB_SYNCHRONIZE=false

# Telemetry is opt-in: with no endpoint the SDK never starts.
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=ledger
OTEL_SDK_DISABLED=
